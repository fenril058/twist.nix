;;; early-init.el --- Early init for twist  -*- lexical-binding: t; -*-

;; Packages are managed by Nix, so package.el must not activate at startup.
(setq package-enable-at-startup nil)

;;; early-init.el ends here
